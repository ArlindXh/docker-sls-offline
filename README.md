## Serverless-offline with Docker

A simple setup for local development.
Emulating API Gateway and Lambdas locally with docker.

### Usage

Pre-setup:
`docker-compose up --build`

Endpoint:
`http://localhost:3000`
